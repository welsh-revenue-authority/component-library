import {
  Meta,
  Title,
  Primary,
  Controls,
  Stories
} from "@storybook/addon-docs/blocks";

import * as ButtonStories from "../stories/Button.stories";

<Meta of={ButtonStories} />

<Title />

<Primary />

<Controls />

Refer to [Icon component](/docs/images-icons-icon--icondocs) for more information on `prepend-icon` and `append-icon`

`prepend-icon` displays a prepend icon slot <u>before</u> the button content.

`append-icon` displays an append icon slot <u>after</u> the button content.

<Stories />
